# Python build artifacts
__pycache__/
*.py[cod]
*$py.class

# uv / virtualenv (env is machine-specific; uv.lock is committed for reproducibility)
.venv/

# Working notes / external cross-check material (not part of the deliverable)
gemini_say.txt
AUDIT*.md

# Reproduce scratch (regenerated on demand)
_repro_backup/
training_curves_*.npz
demo_data.json

# OS / editor cruft
.DS_Store
Thumbs.db

.claude